- name: Configure MOTD Banner
  hosts: all
  connection: network_cli
  gather_facts: false

  tasks:
    - name: Set the MOTD banner
      cisco.ios.ios_banner:
        banner: motd
        text: "{{ motd_message }}"
        state: present